#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: 'Program to print to stdout using printf'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	printf("%s\n", "with proper grammar, but the outcome is a piece of art,");
	return (0);
}
